vich_uploader:
    db_driver: orm

    mappings:
        product_images:
            uri_prefix:         '%app.path.guestbookpost_images%'
            upload_destination: '%kernel.project_dir%/public%app.path.guestbookpost_images%'

